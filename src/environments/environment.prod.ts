import { DEFAULT_CONFIG } from '../config/default';

export const environment = {
  production: true,
  appUrl: 'http://juniorviec.com',
  apiEndpoint: 'https://juniorviec.com:1337',
  appName: DEFAULT_CONFIG.appName,
  config: DEFAULT_CONFIG
};
