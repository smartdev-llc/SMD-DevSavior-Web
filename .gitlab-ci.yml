image: node:latest

cache:
  paths:
    - node_modules/

stages:
  - deploy


deploy:
  stage: deploy
  before_script:
    - apt-get update -qq && apt-get install -y -qq sshpass

  script:
    - npm install
    - ./node_modules/@angular/cli/bin/ng build --progress false --prod
    - cd dist/
    - ls
    - sshpass -V
    - export SSHPASS=$USER_PASS 
    - sshpass -p 'Mbxfd1Wm' ssh -o StrictHostKeyChecking=no root@164.132.226.137 "cd /root/juniorviec/juniorviec-web; git pull; npm i; pm2 delete juniorviec-web; ng build --prod; pm2 start pm2.json;"
   
  environment: staging
  only:
    - master
